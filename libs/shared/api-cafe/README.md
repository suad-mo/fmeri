# api-cafe

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build api-cafe` to build the library.

## Running unit tests

Run `nx test api-cafe` to execute the unit tests via [Jest](https://jestjs.io).
