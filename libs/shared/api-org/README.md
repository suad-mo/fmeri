# api-org

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build api-org` to build the library.

## Running unit tests

Run `nx test api-org` to execute the unit tests via [Jest](https://jestjs.io).
